# Build de producción (PWA descargable). La app apunta al backend por defecto,
# así el cliente que instala la app solo escribe correo y clave para conectarse.
# El demo sigue siendo LOCAL: la nube solo se activa tras iniciar sesión (token).
VITE_API_URL=https://ventanilla-api-vvzh.onrender.com
